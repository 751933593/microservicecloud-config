spring:
  profiles:
    active:
    - dev

---
spring:
  profiles: dev #开发环境
  application:
    name: microservicecloud-config-capture-dev

---
spring:
  profiles: test #测试环境
  application:
    name: microservicecloud-config-capture-test

#保存为utf-8
